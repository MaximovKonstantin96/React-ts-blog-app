import React, { FC } from "react";
import MainLayout from "./../../layouts/MainLayout";

const ProfilePage: FC = () => {
  return <MainLayout>Здесь будет профайл</MainLayout>;
};

export default ProfilePage;
